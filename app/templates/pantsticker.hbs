<div class="btn btn-toolbar hidden-print" style="margin-top:30px">
  <button class="btn btn-primary" type="button" {{action "print"}}>Print</button>
  <button class="btn btn-default" type="button" {{action "clear"}}>Clear</button>
  <button class="btn btn-default" type="button" {{action "addItem"}}>Add Item</button>
</div>
<table class="table hidden-print">
  <thead>
    <tr>
      <th> Size </th>
      <th> Fit </th>
      <th> Price </th>
      <th> Quantity </th>
    </tr>
  </thead>
  <tbody>
    {{#each items as |item|}}
      <tr>
        <td><Input @value={{item.size}} class="form-control" @autofocus={{true}} @autocomplete="on" /></td>
        <td><Input @value={{item.fit}} class="form-control" @autocomplete="on" /></td>
        <td><Input @value={{item.price}} class="form-control" @autocomplete="on" /></td>
        <td><Input @value={{item.qty}} class="form-control" @autocomplete="on" /></td>
      </tr>
    {{/each}}
  </tbody>
</table>
<PantStickerList @items={{printitems}} />