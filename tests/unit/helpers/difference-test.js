
import { difference } from 'royal-pos/helpers/difference';
import { module, test } from 'qunit';

module('Unit | Helper | difference', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = difference([42]);
    assert.ok(result);
  });
});

