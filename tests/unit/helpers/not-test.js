
import { not } from 'royal-pos/helpers/not';
import { module, test } from 'qunit';

module('Unit | Helper | not', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = not([42]);
    assert.ok(result);
  });
});

