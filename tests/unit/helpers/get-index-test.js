
import { getIndex } from 'royal-pos/helpers/get-index';
import { module, test } from 'qunit';

module('Unit | Helper | get index', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getIndex([42]);
    assert.ok(result);
  });
});

